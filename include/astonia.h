/*
 * Basic Astonia types for mod development
 */

#ifndef ASTONIA_H
#define ASTONIA_H

#include <stdint.h>
#include <stddef.h>

typedef uint32_t tick_t;       // SDL ticks, timestamps
typedef size_t map_index_t;    // Map tile index, selection indices

#endif /* ASTONIA_H */
